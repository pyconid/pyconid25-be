name: CI/CD Pipeline

on: 
  push:
    branches: [main, dev]
  workflow_dispatch: 

jobs:
  format-lint-test:
    name: Format, Lint and Test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Display Python version
        run: python -c "import sys; print(sys.version)"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Check code formatting with Ruff
        run: ruff format --check
      - name: Lint code with Ruff
        run: ruff check --exit-non-zero-on-fix
      - name: Test with pytest
        env:
          ENVIRONTMENT: os
          CORS_ALLOWED_ORIGINS: http://127.0.0.1/,https://www.google.com/,https://fastapi.tiangolo.com/
          SECRET_KEY: githubtest
          ALGORITHM: HS256
          ACCESS_TOKEN_EXPIRE_MINUTES: 1440
          REFRESH_TOKEN_EXPIRE_MINUTES: 2880
          TZ: Asia/Jakarta
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_PASSWORD: postgres
          POSTGRES_DATABASE: postgres
          POSTGRES_USER: postgres
        run: |
          alembic upgrade head
          pytest . -n auto

  docker-build:
    name: Build Docker Image
    needs: format-lint-test
    runs-on: ubuntu-latest
    env:
      REGISTRY: ghcr.io
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: version
        run: echo "version=$(cat version.txt)" >> $GITHUB_OUTPUT

      # Load .env dari secret sesuai branch
      - name: Load .env for build context (dev)
        if: github.ref == 'refs/heads/dev'
        run: echo "${{ secrets.DEV_ENV_FILE }}" > .env

      - name: Load .env for build context (main/prod)
        if: github.ref == 'refs/heads/main'
        run: echo "${{ secrets.PROD_ENV_FILE }}" > .env

      - name: Export variables from .env
        run: |
          set -a
          source .env
          set +a
          env | grep GHCR_USERNAME
          env | grep IMAGE_NAME
          env | grep IMAGE_TAG

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u "${{ secrets.GHCR_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            -t ghcr.io/${GHCR_USERNAME}/${IMAGE_NAME}:${{ steps.version.outputs.version }} \
            -t ghcr.io/${GHCR_USERNAME}/${IMAGE_NAME}:latest \
            .

      - name: Push Docker image
        run: |
          docker push ghcr.io/${GHCR_USERNAME}/${IMAGE_NAME}:${{ steps.version.outputs.version }}
          docker push ghcr.io/${GHCR_USERNAME}/${IMAGE_NAME}:latest

      - name: Remove Docker image from Runner
        run: |
          docker rmi ghcr.io/${GHCR_USERNAME}/${IMAGE_NAME}:${{ steps.version.outputs.version }}
          docker rmi ghcr.io/${GHCR_USERNAME}/${IMAGE_NAME}:latest

      - name: Logout Docker
        run: |
          docker logout ghcr.io

  deploy-dev:
    name: Deploy to Development
    needs: [docker-build]
    if: github.ref == 'refs/heads/dev' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment:
      name: development
      url: https://dev-api.pycon.id

    steps:
      - uses: actions/checkout@v4
      
      - name: Read version
        id: version
        run: echo "version=$(cat version.txt)" >> $GITHUB_OUTPUT
        
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          
      - name: Setup known_hosts
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEV_SSH_KNOWN_HOSTS }}" >> ~/.ssh/known_hosts
      
      - name: Copy docker-compose.yaml and version.txt to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USER }}
          port: ${{ secrets.DEV_SSH_PORT || '22' }}
          key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          source: |
            docker-compose.yaml
            version.txt
          target: /home/${{ secrets.DEV_SSH_USER }}/pyconid25-be-dev/
          overwrite: true

      - name: SSH - Generate .env file dari secret
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USER }}
          port: ${{ secrets.DEV_SSH_PORT || '22' }}
          key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          script: |
            cd ~/pyconid25-be-dev
            echo "${{ secrets.DEV_ENV_FILE }}" > .env
            chmod 600 .env

            # Update IMAGE_TAG di .env sesuai version.txt
            VERSION=$(cat version.txt)
            if grep -q '^IMAGE_TAG=' .env; then
              sed -i "s/^IMAGE_TAG=.*/IMAGE_TAG=$VERSION/" .env
            else
              echo "IMAGE_TAG=$VERSION" >> .env
            fi

            # Login ke GHCR pakai akun premium
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u "${{ secrets.GHCR_USERNAME }}" --password-stdin

            # Pull dan deploy
            docker compose --env-file .env pull
            docker compose --env-file .env up -d

  deploy-prod:
    name: Deploy to Production
    needs: [docker-build]
    if: (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')) && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://api.pycon.id

    steps:
      - uses: actions/checkout@v4
      
      - name: Read version
        id: version
        run: echo "version=$(cat version.txt)" >> $GITHUB_OUTPUT
        
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          
      - name: Setup known_hosts
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.PROD_SSH_KNOWN_HOSTS }}" >> ~/.ssh/known_hosts
      
      - name: Copy docker-compose.yaml and version.txt to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USER }}
          port: ${{ secrets.PROD_SSH_PORT || '22' }}
          key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          source: |
            docker-compose.yaml
            version.txt
          target: /home/${{ secrets.PROD_SSH_USER }}/pyconid25-be-prod/
          overwrite: true

      - name: SSH - Generate .env file dari secret
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USER }}
          port: ${{ secrets.PROD_SSH_PORT || '22' }}
          key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          script: |
            cd ~/pyconid25-be-prod
            echo "${{ secrets.PROD_ENV_FILE }}" > .env
            chmod 600 .env

            # Update IMAGE_TAG di .env sesuai version.txt
            VERSION=$(cat version.txt)
            if grep -q '^IMAGE_TAG=' .env; then
              sed -i "s/^IMAGE_TAG=.*/IMAGE_TAG=$VERSION/" .env
            else
              echo "IMAGE_TAG=$VERSION" >> .env
            fi

            # Login ke GHCR pakai akun premium
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u "${{ secrets.GHCR_USERNAME }}" --password-stdin

            # Pull dan deploy
            docker compose --env-file .env pull
            docker compose --env-file .env up -d